import React from 'react';
import ReactDOM from 'react-dom/client';
import ChickenTracks from './ChickenTracks_v5';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ChickenTracks />
  </React.StrictMode>
);
